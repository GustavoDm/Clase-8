#include <stdio.h>
#include <stdlib.h>

int get_Estructura(*EPersona cliente, int cantidad)
{

int i;

 for(i=0;i<cantidad;i++){
 printf("Ingrese su nombre: ");
 gets(*cliente.Nombre);
 /*fflush(stdin);
 printf("\nIngrese su Apellido: ");
 gets(Apellido);
 fflush(stdin);
 printf("\nIngrese su DNI: ");
 scanf("%d", DNI);/*/
 }

return 0;
}
